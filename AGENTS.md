# Agent instructions

Keep this project strictly public-data-only. Never add wallet connectivity, secret configuration, signing, authenticated trading APIs, order submission, transfers, approvals, or geographic bypass. Financial calculations must remain Decimal-based. Do not mark live verification as successful unless the public endpoints were actually reached in that run. Run lint, tests, and the security scanner before commits.
